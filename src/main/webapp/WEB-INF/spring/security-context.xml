<beans:beans xmlns="http://www.springframework.org/schema/security" xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:mvc="http://www.springframework.org/schema/mvc"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
    http://www.springframework.org/schema/security
    http://www.springframework.org/schema/security/spring-security-3.1.xsd
    http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-3.1.xsd">

    <http auto-config="true" use-expressions="true">
        <form-login login-page="/login" login-processing-url="/j_spring_security_check" authentication-failure-url="/login?login_error=t" />
        <logout logout-url="/j_spring_security_logout" />
        
        <intercept-url pattern="/reportDocumentUploadFailure" access="permitAll" />
        <intercept-url pattern="/resendApplicationToPortico" access="permitAll" />
        
        <intercept-url pattern="/autosuggest/**" access="hasAnyRole('ADMINISTRATOR','REVIEWER', 'APPLICANT', 'APPROVER', 'SUPERADMINISTRATOR', 'INTERVIEWER', 'REFEREE', 'SUPERVISOR', 'VIEWER')" />
        
        <intercept-url pattern="/login" access="permitAll" />
        <intercept-url pattern="/forgotPassword/**" access="permitAll" />
        <intercept-url pattern="/design/**" access="permitAll" />
        <intercept-url pattern="/programs" access="permitAll" />
        <intercept-url pattern="/register/**" access="permitAll" />
        <intercept-url pattern="/addReferences/**" access="permitAll" />
        <intercept-url pattern="/referee/register/**" access="permitAll" />
        <intercept-url pattern="/refereeRegistration/**" access="permitAll" />
        <intercept-url pattern="/decline/**" access="permitAll" />
        <intercept-url pattern="/references/**" access="permitAll" />
        <intercept-url pattern="*/j_spring_security_check" access="permitAll" />
        <intercept-url pattern="/apply/**" access="hasRole('APPLICANT')" />
        <intercept-url pattern="/submit/**" access="hasRole('APPLICANT')" />
        <intercept-url pattern="/update/**" access="hasRole('APPLICANT')" />
        <intercept-url pattern="/assignReviewers/**" access="hasAnyRole('ADMINISTRATOR','SUPERADMINISTRATOR','REVIEWER')" />
        <intercept-url pattern="/rejectApplication/**" access="hasAnyRole('ADMINISTRATOR','SUPERADMINISTRATOR','APPROVER')" />
        <intercept-url pattern="/reviewer/**" access="hasAnyRole('ADMINISTRATOR','REVIEWER','APPROVER', 'SUPERADMINISTRATOR')" />
        <intercept-url pattern="/**" access="hasAnyRole('ADMINISTRATOR','REVIEWER', 'APPLICANT', 'APPROVER', 'SUPERADMINISTRATOR', 'INTERVIEWER', 'REFEREE', 'SUPERVISOR', 'VIEWER', 'ADMITTER')" />
        <intercept-url pattern="/dwr/**" access="permitAll" />
        
        <session-management session-fixation-protection="migrateSession" />
    </http>

    <authentication-manager>
        <authentication-provider ref="authenticationProvider" />
    </authentication-manager>

    <beans:bean id="authenticationProvider" class="com.zuehlke.pgadmissions.security.PgAdmissionAuthenticationProvider" autowire-candidate="false">
        <beans:constructor-arg index="0" ref="userDetailsService" />
        <beans:constructor-arg index="1" ref="authenticationEncryptionUtils" />
    </beans:bean>
    <beans:bean id="userDetailsService" class="com.zuehlke.pgadmissions.security.PgAdmissionUserDetailsService" />
    <beans:bean id="authenticationEncryptionUtils" class="com.zuehlke.pgadmissions.utils.EncryptionUtils" autowire-candidate="false" />

</beans:beans>