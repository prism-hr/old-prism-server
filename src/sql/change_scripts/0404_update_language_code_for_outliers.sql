UPDATE LANGUAGE SET code = "CN" WHERE name LIKE "China%"
;

UPDATE LANGUAGE SET code = "HK" WHERE name LIKE "Hong Kong%"
;

UPDATE LANGUAGE SET code = "CD" WHERE name LIKE "Congo%"
;

UPDATE COUNTRIES SET code = "CN" WHERE name LIKE "China%"
;

UPDATE COUNTRIES SET code = "CD" WHERE name LIKE "Congo%"
;